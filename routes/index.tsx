import { define } from "../utils.ts";

export default define.page(function Home() {
  return (
    <div class="min-h-screen flex items-center justify-center">
      <h1 class="text-4xl font-bold">Rift Reunion</h1>
    </div>
  );
});
